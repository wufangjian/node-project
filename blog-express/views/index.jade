extends layout

block page
    - var menu = 'index'
block content
    if (articles.length === 0)
        | There`s no publicshed content yet.
        a(href="/login") Login in
        | to post and publish.
    else
        each article, index in articles
            div
                h2
                    a(href="/articles/#{article.slug}")= article.title